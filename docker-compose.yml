version: "3"
services:
  emailer2:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: emailer2
    ports:
      - "1001:1001"
    restart: always
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
